# Production Environment Configuration
# Rename this file to .env on the production machine

# Database (SQLite for local storage as requested)
DATABASE_URL=sqlite:///./sql_app.db

# Security (CHANGE THESE ON PRODUCTION)
SECRET_KEY=change_this_to_a_long_random_string_for_production_security
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200 # 30 Days (Long login for "Always-On")

# Dashboard Static Path (Optional override)
# DASHBOARD_PATH=../dashboard
